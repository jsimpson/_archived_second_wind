- provide(:title, 'New Activity')
%h1 New Activity
.span6.offset3
  = form_for @activity, html: { class: 'form-horizontal' } do |f|
    = render 'shared/activity_error_messages'
    .form-group
      .col-sm-offset-2.col-sm-10
        = f.submit 'Create', class: "btn btn-primary", id: "submit-button"
