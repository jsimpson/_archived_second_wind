$('#activities').html('#{escape_javascript (render partial: 'activity', locals: { activities: @activities } )}');
$('#paginator').html('#{escape_javascript(paginate(@activities, remote: true).to_s)}');
