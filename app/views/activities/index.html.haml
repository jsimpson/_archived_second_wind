= javascript_include_tag "//www.google.com/jsapi", "chartkick"

- provide(:title, 'Activities')

%h1 Activities

.row
  - if @activities.any?
    = render :partial => 'tabs_nav'
    = render :partial => 'tabs_content', locals: { activities: @activities, lifetime: @lifetime, last_year: @last_year, last_month: @last_month, last_week: @last_week }
